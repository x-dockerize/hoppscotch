SERVER_HOSTNAME=console.example.com

#-----------------------Backend Config------------------------------#
# DATABASE
DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_NAME=hoppscotch
DATABASE_USER=hoppscotch
DATABASE_PASSWORD=

# Sensitive Data Encryption Key while storing in Database (32 character)
DATA_ENCRYPTION_KEY=

# Whitelisted origins for the Hoppscotch App.
# This list controls which origins can interact with the app through cross-origin comms.
# - localhost ports (3170, 3000, 3100): app, backend, development servers and services
# - app://localhost_3200: Bundle server origin identifier
#   NOTE: `3200` here refers to the bundle server (port 3200) that provides the bundles,
#   NOT where the app runs. The app itself uses the `app://` protocol with dynamic
#   bundle names like `app://{bundle-name}/`
WHITELISTED_ORIGINS=https://console.example.com

#-----------------------Frontend Config------------------------------#
# Base URLs
VITE_BASE_URL=https://console.example.com
VITE_SHORTCODE_BASE_URL=https://console.example.com
VITE_ADMIN_URL=https://console.example.com/admin

# Backend URLs
VITE_BACKEND_GQL_URL=https://console.example.com/graphql
VITE_BACKEND_WS_URL=wss://console.example.com/graphql
VITE_BACKEND_API_URL=https://console.example.com/v1

# Terms Of Service And Privacy Policy Links (Optional)
VITE_APP_TOS_LINK=https://docs.hoppscotch.io/support/terms
VITE_APP_PRIVACY_POLICY_LINK=https://docs.hoppscotch.io/support/privacy

## Required for subpath-based access. Don't change this!
ENABLE_SUBPATH_BASED_ACCESS=true
